<form style="display: inline-block" method="post" action="{{ path('warmup_plan_delete', {'id': warmupPlan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ warmupPlan.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
